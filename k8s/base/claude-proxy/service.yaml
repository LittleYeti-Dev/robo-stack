apiVersion: v1
kind: Service
metadata:
  name: claude-proxy
  labels:
    app: claude-proxy
spec:
  type: ClusterIP
  selector:
    app: claude-proxy
  ports:
    - port: 8080
      targetPort: 8080
      name: http
