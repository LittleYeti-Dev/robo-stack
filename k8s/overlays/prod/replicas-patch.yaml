apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-proxy
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: claude-proxy
          resources:
            limits:
              cpu: 1000m
              memory: 1Gi
